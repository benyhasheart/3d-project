#include "VertexConstantBuffer.h"

//template<typename C>
//void Update(Graphics& graphics, const C& constantData)
//{
//	graphics.GetDeviceContext()->UpdateSubresource(mConstantBuffer.get()->GetBuffer(), 0, nullptr, &ownerTansform, 0, 0);
//}